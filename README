2 python files;

aws_ips.py will pull all IPs from eu-west-1 and eu-west-2 from the public AWS API into a file call aws_ips.txt

populateVRconfig will configure a Palo Virtual Router with the contents of that file where
 - Virtual Router name : default
 - BGP Import rule name : my_prefix_list
